# META GAME ENGINE - MVP

Полнофункциональный MVP игрового движка на языке Rust, объединяющий метаязык для описания игровых сущностей с архитектурой, вдохновленной Godot Engine.

![Status](https://img.shields.io/badge/status-MVP-green)
![Rust](https://img.shields.io/badge/Rust->=1.70-blue)
![License](https://img.shields.io/badge/license-MIT-green)

## 🎮 Основные возможности

- **Meta Language Integration** -定義 сущностей через DSL (Domain-Specific Language)
- **Scene System** - Иерархическая система сцен и объектов (как в Godot)
- **Component Architecture** - Гибкая система компонентов
- **Event System** - Система событий и обработчиков
- **State Machine** - Управление состояниями объектов
- **Behavior Trees** - Система искусственного интеллекта
- **Runtime Execution** - Прямое выполнение событий на основе метаязыка

## 🏗️ Архитектура

```
┌─────────────────────────────────────┐
│        META GAME ENGINE             │
├─────────────────────────────────────┤
│                                     │
│  ┌──────────────────────────────┐   │
│  │   Meta Language Parser       │   │
│  │   (парсит .meta файлы)       │   │
│  └──────────────────────────────┘   │
│                ↓                    │
│  ┌──────────────────────────────┐   │
│  │    Game Engine Core          │   │
│  │  - Scene Management          │   │
│  │  - Node Hierarchy            │   │
│  │  - Game Loop                 │   │
│  └──────────────────────────────┘   │
│                ↓                    │
│  ┌──────────────────────────────┐   │
│  │    Component System          │   │
│  │  - Transform                 │   │
│  │  - Physics                   │   │
│  │  - Health                    │   │
│  │  - Sprite                    │   │
│  │  - Custom Components         │   │
│  └──────────────────────────────┘   │
│                ↓                    │
│  ┌──────────────────────────────┐   │
│  │    Systems & Managers        │   │
│  │  - Event System              │   │
│  │  - State Machine             │   │
│  │  - Behavior Trees            │   │
│  │  - Scene Manager             │   │
│  └──────────────────────────────┘   │
│                ↓                    │
│  ┌──────────────────────────────┐   │
│  │    Runtime Executor          │   │
│  │  - Event Execution           │   │
│  │  - Value Management          │   │
│  │  - State Updates             │   │
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
```

## 📁 Структура проекта

```
experiment/
├── Cargo.toml
├── src/
│   ├── main.rs              # Демонстрация MVP
│   ├── meta_lang.rs         # Парсер метаязыка
│   ├── game_engine.rs       # Основной движок (Scene, Node, Component)
│   ├── components.rs        # Встроенные компоненты
│   ├── runtime.rs           # Runtime для выполнения событий
│   ├── systems.rs           # Event System, State Machine, Behavior Tree
│   ├── scene.rs             # Менеджер сцен
│   ├── analyzer.rs          # Анализ corpus
│   ├── ast.rs               # AST структуры
│   ├── supercompiler.rs     # Оптимизация
│   └── aot_generator.rs     # AOT генератор
├── MVP_ARCHITECTURE.md      # Подробная архитектура
├── EXAMPLES.md              # Примеры использования
└── README.md                # Этот файл
```

## 🚀 Быстрый старт

### 1. Клонирование и подготовка

```bash
cd experiment
cargo build --release
```

### 2. Запуск демонстрации

```bash
cargo run
```

**Вывод:**
```
=== META GAME ENGINE MVP ===

--- Part 1: Meta Language Parsing ---
Parsed 1 entities: [Entity { name: "Player", components: [...], events: [...] }, ...]

--- Part 2: Runtime Execution ---
Player moves by 0.8 (velocity 5 * dt 0.16)
Player takes 10 damage, health -> 90

--- Part 3: Game Engine MVP ---
✓ Scene created from meta language
✓ Scene loaded
✓ Game objects created: Player, Enemy
✓ Starting game loop simulation (5 frames)...
Frame 0: Time=0.016s Root children=2 Player, Enemy
...
✓ Game engine MVP demonstration complete
```

## 📝 Метаязык - Синтаксис

### Базовая сущность

```rust
entity PlayerCharacter {
    components: [Transform, Sprite, Physics, Health, Input];
    
    on Update(dt) {
        // Выполняется каждый кадр
        move(velocity * dt);
        check_collisions();
    }
    
    on Collision(other) {
        // Обработка столкновения
        if (other.tag == "Enemy") {
            takeDamage(20);
        }
    }
    
    on TakeDamage(amount) {
        // Кастомное событие
        health -= amount;
        playHitAnimation();
    }
}
```

### Компоненты

```rust
components: [
    Transform,      // Позиция, ротация, масштаб
    Sprite,         // Графическое отображение
    Physics,        // Физика (скорость, масса)
    Health,         // Здоровье и урон
    Input,          // Обработка ввода
    AI,             // Искусственный интеллект
    Audio,          // Звук
    Collider        // Физический коллайдер
]
```

## 💻 Примеры кода

### Создание игры

```rust
use game_engine::GameEngine;

fn main() {
    let mut engine = GameEngine::new();
    
    // Создать сцену из метаязыка
    let meta = r#"
        entity Player {
            components: [Transform, Sprite, Physics];
            on Update(dt) { move(velocity * dt); }
        }
    "#;
    
    engine.create_scene_from_meta("MainScene", meta).unwrap();
    engine.load_scene("MainScene").unwrap();
    
    // Game loop
    while engine.is_running() {
        engine.update(0.016); // 60 FPS
    }
}
```

### Работа с компонентами

```rust
let mut player = Node::new("player", "Player", "Character");

let mut transform = Component::new("Transform");
transform.set_property("position", Value::Float(0.0));
player.add_component(transform);

let mut health = Component::new("Health");
health.set_property("hp", Value::Int(100));
player.add_component(health);
```

### State Machine

```rust
let mut state_machine = StateMachine::new();

state_machine.add_transition(
    EntityState::Idle,
    "move".to_string(),
    EntityState::Moving,
);

state_machine.on_event("move");
assert!(state_machine.is_in_state(&EntityState::Moving));
```

Более подробные примеры см. в [EXAMPLES.md](EXAMPLES.md)

## 🛠️ Основные компоненты

### GameEngine
```rust
pub struct GameEngine {
    pub scenes: HashMap<String, Scene>,
    pub current_scene: Option<String>,
    pub time: GameTime,
}
```

**Методы:**
- `create_scene_from_meta()` - Создание сцены из метаязыка
- `load_scene()` - Загрузка сцены
- `update(delta_time)` - Обновление кадра
- `add_node()` - Добавление объекта в сцену

### Node (базовый объект)
```rust
pub struct Node {
    pub id: String,
    pub name: String,
    pub components: HashMap<String, Component>,
    pub children: Vec<Node>,
    pub active: bool,
}
```

**Методы:**
- `add_component()` - Добавить компонент
- `add_child()` - Добавить дочерний объект
- `find_child()` - Найти дочерний объект

### Встроенные компоненты
- **Transform** - Позиция (position), ротация (rotation), масштаб (scale)
- **Health** - take_damage(), heal(), is_alive()
- **Physics** - Скорость, масса, гравитация
- **Sprite** - Текстура, видимость
- **Input** - Обработка ввода
- **Velocity** - Вектор скорости

## 📊 Выполнение игрового цикла

```
┌─────────────────────────────┐
│   Game Loop (每フレーム)    │
└──────────────┬──────────────┘
               │
      ┌────────▼────────┐
      │  engine.update()  │
      └────────┬────────┘
               │
      ┌────────▼────────┐
      │ Update GameTime  │
      └────────┬────────┘
               │
      ┌────────▼────────┐
      │ Update all Nodes │
      └────────┬────────┘
               │
      ┌────────▼────────────────────┐
      │ Execute Events for each Node │
      │ (on Update, on Collision..)  │
      └────────┬────────────────────┘
               │
      ┌────────▼────────┐
      │ Update children  │
      │  (recursively)   │
      └────────┬────────┘
               │
      ┌────────▼────────┐
      │    Next Frame    │
      └─────────────────┘
```

## 🧪 Тестирование

Проект включает примеры тестов в [src/meta_lang.rs]:

```bash
cargo test
```

## 📈 Development Roadmap

### ✅ Готово
- [x] Meta Language Parser
- [x] Game Engine Core (Scene, Node, Component)
- [x] Runtime Execution
- [x] Event System (готова)
- [x] State Machine (готова)
- [x] Behavior Trees (готова)
- [x] MVP демонстрация

### ⏳ Планируется

**Phase 2: Базовые системы**
- [ ] 2D Physics Engine (collision detection, rigidbodies)
- [ ] Sprite Rendering System
- [ ] Input Handling (keyboard, mouse, gamepad)
- [ ] Audio System
- [ ] Animation System
- [ ] Particle System

**Phase 3: Продвинутые возможности**
- [ ] Visual Editor (как в Godot)
- [ ] Asset Management
- [ ] Save/Load System
- [ ] Networking & Multiplayer
- [ ] Debugging Tools

**Phase 4: Performance & Optimization**
- [ ] ECS (Entity Component System) для большого количества объектов
- [ ] Spatial partitioning для оптимизации поиска
- [ ] Compile-time optimizations
- [ ] Profiling tools

## 📚 Документация

- **[MVP_ARCHITECTURE.md](MVP_ARCHITECTURE.md)** - Подробная архитектура всех компонентов
- **[EXAMPLES.md](EXAMPLES.md)** - Практические примеры использования
- **Встроенная документация** - `cargo doc --open`

## 🤝 Контрибьютинг

Приглашаем к участию! Основные области для развития:

1. **Физика** - Реализация 2D/3D физики
2. **Рендеринг** - Графический движок
3. **Ввод** - Input handling система
4. **Инструменты** - Визуальный редактор

## 📝 Лицензия

MIT License - см. LICENSE файл

## 🎯 Цель проекта

Создание модульного, расширяемого game engine'а на Rust, который:
- Использует метаязык для декларативного описания сущностей
- Приввожет архитектуру, похожую на Godot
- Обеспечивает высокую производительность
- Легко расширяется кастомными компонентами и системами

## 📞 Связь

Для вопросов и предложений обратитесь в Issues или создайте Pull Request.

---

**Статус:** MVP готов к использованию и расширению! 🚀
