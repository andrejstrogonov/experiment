{
  "name": "experiment-web-tests",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "playwright": "^1.36.0"
  },
  "scripts": {
    "build": "powershell -File ./build.ps1",
    "test": "playwright test --project=chromium"
  }
}